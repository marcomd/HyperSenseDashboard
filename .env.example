# Frontend Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# Tunnel Configuration (for remote access during development)
# =============================================================================

# Backend tunnel URL for remote access (optional)
# Used when accessing the frontend via a tunnel service (ngrok, pinggy, etc.)
# Example: api.hypersense.com
#VITE_BACKEND_TUNNEL_URL=https://your-backend-url

# Allowed hosts for Vite dev server (comma-separated, optional)
# Add your frontend tunnel hostname here
# Example: www.hypersense.com
#VITE_ALLOWED_HOSTS=your-frontend-url
